crypto_elt:
  target: dev
  outputs:
    dev:
      type: duckdb
      path: "{{ env_var('DBT_DUCKDB_PATH', 'data/warehouse/crypto_elt.duckdb') }}"
      schema: analytics
      threads: "{{ env_var('DBT_THREADS', '4') | int }}"
